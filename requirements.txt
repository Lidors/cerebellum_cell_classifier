# Core — required for feature extraction
numpy>=1.24
scipy>=1.10
pandas>=2.0
numba>=0.57

# Classifier (Phase 4)
torch>=2.0

# Dimensionality reduction + evaluation
scikit-learn>=1.3
umap-learn>=0.5

# GUI (Phase 3)
pyqt5>=5.15
pyqtgraph>=0.13
matplotlib>=3.7

# Progress bars (optional but recommended)
tqdm>=4.65
